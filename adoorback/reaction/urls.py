from django.urls import path

from reaction import views

urlpatterns = [
    # path('<str:ctype>/<int:oid>/', views.ReactionList.as_view(), name='response-list'),
    # path('<int:pk>/', views.ReactionDestroy.as_view(), name='reaction-destroy'),
]
